Commenter:
<%= dealcomment.user.login %>
<br/>
Comment:
<%= dealcomment.content %>
<br/>
<div>
Posted <%= time_ago_in_words(dealcomment.created_at) %> ago.
<br/>
<% if correctuser?(dealcomment.user) %>
// TODO there seems to be some error in correct_user - check that later
<!-- <%= link_to 'Delete', [dealcomment.deal, dealcomment],
               :confirm => 'Are you sure?',
               :method => :delete %> -->
<% end %>
<br/>
Put an X sign later rather than the message
<br/><br/>

