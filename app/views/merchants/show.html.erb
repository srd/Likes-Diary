<% if isadmin? %>	
	<%= link_to 'Edit', edit_merchant_path %>
<% end %>
<div>
	<div>
		<%= image_tag @merchant.photo.url(:small) %>
	</div>
	<b>Name </b>
	<%= @merchant.merchantname %>	<br/><br/>
	<%= @merchant.merchantinfo %> <br/><br/>
	<%= @merchant.meraddress %> <br/><br/>
	<%= @merchant.merchantphone %> <br/><br/>
	<%= @merchant.merspectext %> <br/><br/>
	<%= @merchant.tribecomment %> <br/><br/>
	<b>City</b><%= @merchant.city.cityname %> <br/><br/>
</div>

	<div>
		<% unless !@merchant.deal1_file_name %>
			<%= image_tag @merchant.deal1.url(:small) %>
		<% end %>
	</div>
	<div>
		<% unless !@merchant.deal2_file_name %>
			<%= image_tag @merchant.deal2.url(:small) %>
		<% end %>
	</div>
	
		<div>
		<% unless !@merchant.speciality1_file_name %>
			<%= image_tag @merchant.speciality1.url(:small) %>
		<% end %>
	</div>
	<div>
		<% unless !@merchant.speciality2_file_name %>
			<%= image_tag @merchant.speciality2.url(:small) %>
		<% end %>
	</div>

Deals offered by this merchant so far<br/>
<%= render @deals %>
<br/><br/><br/><br/>
//add likes here
<%= render 'like_form' if signedin? %>

<br/>

This page has been liked by - <br/>
<%= render @likers %>

//add comments here
<br/>

<% if signedin? %>

	<h2>Comments</h2>
	Count is:
	<%= @merchant.merchantcomments.count %><br/>
	//need to paginate comments later
	<br/><br/>
	<%= render :partial => "merchantcomments/merchantcomment",
           :collection => @comments %>

<% end %>

<br/><br/>

<%= render 'merchants/commentsform' if signedin? %>
