<%= form_for([@product, @product.productcomments.build]) do |f| %>
  <div class="field">
    <%= f.text_area(:content, :size => "70x5")%>
  </div>
  <div class="actions">
    <%= f.submit "Comment" %>
  </div>
<% end %>