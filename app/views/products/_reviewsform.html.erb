<%= form_for([@product, @product.reviews.build]) do |f| %>
  <div class="field">
    <%= f.text_area(:content, :size => "70x5")%>
  </div>
  <div class="actions">
    <%= f.submit "Add Review" %>
  </div>
<% end %>