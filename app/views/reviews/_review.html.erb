Commenter:
<%= review.user.login %>
<br/>
Comment:
<%= review.content %>
<br/>
<div>
Posted <%= time_ago_in_words(review.created_at) %> ago.
<br/>
<% if correct_user?(review.user) %>
<%= link_to 'Delete', [review.product, review],
               :confirm => 'Are you sure?',
               :method => :delete %>
<% end %>
Put an X sign later rather than the message
<br/><br/>

