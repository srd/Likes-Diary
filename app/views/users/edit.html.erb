<h1>Edit my accoun</h1>

<% form_for @user, :url => users_path do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render :partial => 'form', :object => f %>
  <%= f.submit "Update" %>
<% end %>

<br/><%= link_to "My Profile", users_path %>
